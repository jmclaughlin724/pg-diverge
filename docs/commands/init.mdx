---
title: "init"
description: "Scaffold or repair supaschema config and optionally install the reviewed agent bundle."
keywords: ["init command", "scaffold", "repair", "setup"]
---

Use `init` after package install or when setup needs repair.

The command scaffolds config, schema directories, migration directories, and path-confirmation state. It does not install active AI-agent rules, skills, hooks, settings, `AGENTS.md`, or `CLAUDE.md` by default.

## Use this when

- supaschema was installed in a project that has not been set up yet.
- `supaschema.config.json` should be repaired from the canonical contract.
- The user has reviewed `node_modules/supaschema/agent-bundle/INSTALL.md` and explicitly asks to install AI-agent enforcement with `--agent-bundle`.

## Run it

```bash
<local-runner> init
<local-runner> init --agent-bundle
<local-runner> init --dry-run --json
<local-runner> init --repair
```

Existing config files are left untouched unless `--repair` is passed.

## Flags

<ParamField path="--dry-run" type="boolean">
  Print the scaffold or repair plan without writing files.
</ParamField>

<ParamField path="--json" type="boolean">
  Print the init result as redacted JSON.
</ParamField>

<ParamField path="--repair" type="boolean">
  Rewrite `supaschema.config.json` from the canonical contract when needed.
</ParamField>

<ParamField path="--agent-bundle" type="boolean">
  Install the reviewed raw AI-agent bundle into `.agents`, `.claude`, `.codex`,
  `AGENTS.md`, and `CLAUDE.md`. Do not use this flag unless the user asks for
  AI-agent enforcement or approves the bundle.
</ParamField>

## Related

<CardGroup cols={2}>
  <Card title="Installation" icon="download" href="/installation">
    Add supaschema to a project.
  </Card>
  <Card title="Setup" icon="wrench" href="/setup">
    Confirm generated paths and files.
  </Card>
  <Card title="What's included" icon="package" href="/whats-included">
    See the package and agent surfaces.
  </Card>
  <Card
    title="Config validate"
    icon="settings"
    href="/commands/config-validate"
  >
    Check the generated config.
  </Card>
</CardGroup>
