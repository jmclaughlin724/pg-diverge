---
title: "sync"
description: "Gate pending migrations, reconcile configured targets, and apply through the selected runner."
keywords: ["sync command", "migration sync", "apply gate", "migrations"]
---

Use `sync` when you want a guarded migration apply path.

Bare `sync` selects every configured `sync.targets.<name>` entry with `mode: "auto"`, reconciles history, checks pending migrations, runs deploy safety gates, then invokes each selected target's runner. Remote deploy uses the same selection rule; it also requires that target's approval variable before any mutation.

## Use this when

- Pending files need `check` before apply.
- Ghost or out-of-order history should block deployment.
- A dry run should preview what the runner would apply.

## Run it

```bash
npx supaschema sync
npx supaschema sync --no-diff
```

`sync.targets` controls target selection. `workflow.migration_sync` only controls whether apply is allowed and whether bare `sync` may select automatic targets: `disabled` refuses apply, `manual` leaves bare `sync` on the dry-run gate, and `auto` lets bare `sync` use targets whose own `mode` is `"auto"`. `--target <name>` is an explicit override for one configured target.

## Flags

<ParamField path="--migrations-dir" type="path">
  Directory containing migration files.
</ParamField>

<ParamField path="--database-url" type="url">
  Target database used for history reconciliation.
</ParamField>

<ParamField path="--target" type="name">
  Override config-selected targets with one configured `sync.targets.<name>` target.
</ParamField>

<ParamField path="--runner" type="direct | supabase-cli">
  Override the configured runner for the selected target.
</ParamField>

<ParamField path="--no-diff" type="boolean">
  Skip schema diff generation and generated TypeScript/Zod refresh. Pending
  migrations are still reconciled and checked.
</ParamField>

<ParamField path="sync.targets.<name>.runner" type="direct | supabase-cli">
  Target runner configured in `supaschema.config.json`.
</ParamField>

## Refusal cases

`sync` stops before handoff when:

- migration history has ghost versions;
- migration files are out of order;
- a pending migration fails `check`;
- the selected runner is unavailable;
- `workflow.migration_sync` disables apply;
- deploy type-safety or RLS-safety gates fail;
- `--env` or `--database-url` attempts to override more than one selected target;
- a remote automatic target lacks its configured runtime approval environment variable.

## Exit codes

| Code | Meaning                                              |
| ---- | ---------------------------------------------------- |
| `0`  | Dry run passed, apply passed, or nothing was pending |
| `1`  | Runtime failure                                      |
| `2`  | Gate failure or inconsistent migration history       |

## Related

<CardGroup cols={2}>
  <Card title="Migrations" icon="list-checks" href="/commands/migrations">
    Inspect applied and pending history first.
  </Card>
  <Card title="Check" icon="shield-check" href="/commands/check">
    See the replay-safety gate `sync` runs.
  </Card>
  <Card
    title="Supabase integration"
    icon="database"
    href="/guides/supabase-integration"
  >
    Understand the Supabase runner boundary.
  </Card>
  <Card
    title="Configuration"
    icon="sliders-horizontal"
    href="/configuration/config-file"
  >
    Set `workflow.migration_sync`.
  </Card>
</CardGroup>
