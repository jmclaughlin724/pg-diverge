---
title: "Introduction"
description: "supaschema turns legacy declarative schema drift into an automated, replay-safe PostgreSQL schema management loop for any provider."
keywords:
  - supaschema
  - PostgreSQL migrations
  - declarative schema
  - schema diff
  - database providers
---

Use this page to decide whether supaschema fits your schema workflow.

supaschema reads PostgreSQL schema intent from SQL files, compares it with another schema source, and renders a replay-safe migration without Docker, a shadow database, or an ORM. The same run can refresh TypeScript types and Zod validators from the declared schema.

## When it helps

- Your repository should be the source of truth for PostgreSQL schema changes.
- You want migrations that can be retried safely after a failed deploy.
- You need policy, type, and generated-output changes visible before a database catches up.
- You use plain PostgreSQL or a hosted provider such as Supabase, Neon, RDS/Aurora, Cloud SQL, AlloyDB, or Azure PostgreSQL.

## Workflow

<Frame caption="supaschema schema workflow">
  <img
    src="/images/concepts/supaschema-flow.svg"
    alt="supaschema workflow that parses schema files, renders a guarded migration, emits TypeScript and Zod outputs, and hands SQL to the database runner"
  />
</Frame>

You edit schema files. `supaschema diff` renders the migration and configured generated outputs. `supaschema check` verifies replay-safety before your normal runner applies the SQL.

For detailed performance, accuracy, and replay-safety results, see [Benchmarks](/benchmarks).

## Start here

<CardGroup cols={2}>
  <Card title="Install" icon="download" href="/installation">
    Add supaschema to the project that owns your schema files.
  </Card>
  <Card title="Quickstart" icon="terminal" href="/quickstart">
    Generate and check one migration from one schema file.
  </Card>
  <Card
    title="Configure"
    icon="sliders-horizontal"
    href="/configuration/config-file"
  >
    Set schema paths, sources, migrations, environments, and workflow defaults.
  </Card>
  <Card title="Evidence" icon="gauge" href="/benchmarks">
    Review benchmark, accuracy, and replay-safety results.
  </Card>
</CardGroup>
